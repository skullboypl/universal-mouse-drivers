# LOCKED — Redragon King Ultra (tray)

Live SKU. Do not change VID/PID, HID report layout, or battery command
bytes unless the user unlocks King Ultra. See `web/src/devices/LOCK.md`.

This folder must stay separate from `Mice/Rampage/BlitzUltimate/`.
